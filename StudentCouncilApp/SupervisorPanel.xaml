<UserControl x:Class="StudentCouncilApp.SupervisorPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#F5F5F5">

    <UserControl.Resources>
        <Style x:Key="TabButtonStyle" TargetType="RadioButton">
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="#ECF0F1"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="5"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsChecked" Value="True">
                    <Setter Property="Background" Value="#3498DB"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="35"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <Border Grid.Row="0" Background="#2C3E50" Margin="0,0,0,15">
            <StackPanel Margin="20">
                <TextBlock Text="👑 Панель управления" 
                           FontSize="24" 
                           FontWeight="Bold" 
                           Foreground="White"
                           HorizontalAlignment="Center"/>
                <TextBlock x:Name="txtSupervisorInfo" 
                           Text="Руководитель направления" 
                           FontSize="14" 
                           Foreground="#BDC3C7"
                           HorizontalAlignment="Center"
                           Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Вкладки -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,15">
            <RadioButton x:Name="tabEvents" Content="📋 Мои мероприятия" IsChecked="True" Style="{StaticResource TabButtonStyle}" Checked="TabEvents_Checked"/>
            <RadioButton x:Name="tabRequests" Content="📝 Заявки" Style="{StaticResource TabButtonStyle}" Checked="TabRequests_Checked"/>
            <RadioButton x:Name="tabDirection" Content="✏️ Карточка направления" Style="{StaticResource TabButtonStyle}" Checked="TabDirection_Checked"/>
            <RadioButton x:Name="tabReports" Content="📊 Отчетность" Style="{StaticResource TabButtonStyle}" Checked="TabReports_Checked"/>
        </StackPanel>

        <!-- Контент вкладок -->
        <Border Grid.Row="2" Background="White" CornerRadius="10" Padding="15" Margin="15">
            <ContentControl x:Name="tabContent"/>
        </Border>
    </Grid>
</UserControl>