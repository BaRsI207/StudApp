<UserControl x:Class="StudentCouncilApp.SupervisorReportsTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="📊 Отчетность по направлениям" 
                   FontSize="18" 
                   FontWeight="Bold"
                   Foreground="#2C3E50"
                   Margin="0,0,0,10"/>

        <TextBlock Grid.Row="1" 
                   Text="Функционал в разработке" 
                   FontSize="14" 
                   Foreground="#7F8C8D"
                   Margin="0,0,0,30"/>

        <!-- Красивая заглушка -->
        <Border Grid.Row="2" 
                Background="#F8F9FA" 
                CornerRadius="10" 
                Padding="30"
                HorizontalAlignment="Center">
            <StackPanel>
                <TextBlock Text="📋" FontSize="48" HorizontalAlignment="Center"/>
                <TextBlock Text="Отчетность будет доступна в ближайшее время" 
                           FontSize="14" 
                           Foreground="#555"
                           TextWrapping="Wrap"
                           TextAlignment="Center"
                           Margin="0,10,0,0"/>
                <TextBlock Text="Здесь вы сможете формировать отчеты по мероприятиям, участникам и статистике" 
                           FontSize="12" 
                           Foreground="#7F8C8D"
                           TextWrapping="Wrap"
                           TextAlignment="Center"
                           Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Предпросмотр статистики -->
        <Border Grid.Row="3" 
                Background="#ECF0F1" 
                CornerRadius="10" 
                Padding="15"
                Margin="0,20,0,0">
            <StackPanel>
                <TextBlock Text="📈 Статистика (предварительная)" 
                           FontSize="14" 
                           FontWeight="Bold"
                           Foreground="#2C3E50"
                           Margin="0,0,0,10"/>
                <TextBlock x:Name="txtStats" 
                           FontSize="12" 
                           Foreground="#555"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>